node_modules
coverage
dist
.DS_Store
.eslintcache
*.log*
*.env*

# rust
native/target
/target

# generated binding packages: the CI-only native dirs under npm/, and the
# wasm artifacts inside the tracked @petrea/wasm manifest dir
npm/*
!npm/wasm
npm/wasm/binding.*

# build staging
wasm
binaries
