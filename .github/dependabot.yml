# all workflow actions are pinned by commit SHA; dependabot files weekly PRs
# to move them forward (one grouped PR, see groups below)
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      actions:
        patterns: ['*']
